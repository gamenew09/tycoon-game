import Log from "@rbxts/log";
import logconfig from "shared/logconfig";

/**
 * The configuration used for client-sided Logs.
 *
 * Currently isn't any different to the shared logs, but we may change that in the future.
 */
export default logconfig;
